[phases.install]
cmds = [
  "python -m venv /venv",
  "/venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "/venv/bin/alembic upgrade head && /venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
